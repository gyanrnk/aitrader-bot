# --- Dashboard + core bot (works for Streamlit deploy AND local, no keys needed) ---
streamlit>=1.36
numpy>=1.26
pandas>=2.1
yfinance>=0.2.40        # real market data (dashboard falls back to mock if it fails)

# --- Optional: only needed for the ML training path / live trading (not the dashboard) ---
# scikit-learn>=1.4     # ML predictor fallback
# lightgbm>=4.0         # gradient-boosted predictor
# anthropic>=0.39       # Claude agents (slow loop)
# ccxt>=4.3             # live crypto execution
# feedparser>=6.0       # news app (see newsapp/)
